name: Release rune-mcp

# Workflow creates a prerelease when any `v*` tag is pushed; maintainer
# later promotes prerelease to release via GitHub
#
# TODO: runespace-sdk requires CGO, but native Github hosted runners for
# Darwin/amd64 and Windows not working well now. We should later check them again

on:
  push:
    tags:
      - 'v*' # trigger on version tags
  workflow_dispatch:
    inputs:
      version:
        description: 'Existing git tag to build from (e.g. v0.1.0)'
        required: true
        type: string
      dry_run:
        description: 'Build and smoke-test but skip publishing prerelease'
        required: false
        type: boolean
        default: false

permissions:
  contents: write # softprops/action-gh-release

jobs:
  build:
    name: Build ${{ matrix.os }}/${{ matrix.arch }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { runner: ubuntu-22.04,     os: linux,  arch: amd64 }
          - { runner: ubuntu-22.04-arm, os: linux,  arch: arm64 }
          - { runner: macos-14,         os: darwin, arch: arm64 }
    steps:
      # Setup
      - name: Resolve version
        id: version
        shell: bash
        env:
          DISPATCH_VERSION: ${{ inputs.version }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="$DISPATCH_VERSION"
          else
            VERSION="${GITHUB_REF#refs/tags/}"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Building $VERSION for ${{ matrix.os }}/${{ matrix.arch }}"

      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.version.outputs.version }}
          fetch-depth: 0
          fetch-tags: true

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true

      # rune-mcp links runespace-sdk via CGO, so the linux binary depends on
      # the build image's glibc. Name the asset by the build OS (ubuntu-2204 /
      # mac-14), read from the running image, so its glibc/SDK baseline is
      # visible. The linux runners are pinned to 22.04 for broad glibc compat.
      - name: Resolve asset name
        id: osinfo
        shell: bash
        run: |
          set -euo pipefail
          case "${RUNNER_OS}" in
            Linux)
              . /etc/os-release
              label="${ID}-${VERSION_ID//./}" ;;          # ubuntu-2204
            macOS)
              label="mac-$(sw_vers -productVersion | cut -d. -f1)" ;;  # mac-14
            *)
              echo "::error::unhandled RUNNER_OS=${RUNNER_OS}" >&2; exit 1 ;;
          esac
          echo "asset=rune-mcp-${label}-${{ matrix.arch }}" >> "$GITHUB_OUTPUT"

      # Build and test
      - name: Build binary
        shell: bash
        env:
          VERSION: ${{ steps.version.outputs.version }}
          GOOS: ${{ matrix.os }}
          GOARCH: ${{ matrix.arch }}
        run: |
          ASSET="${{ steps.osinfo.outputs.asset }}"
          mkdir -p dist
          go build -trimpath \
            -ldflags "-s -w -X main.version=${VERSION}" \
            -o "dist/${ASSET}" \
            ./cmd/rune-mcp
          ls -lh "dist/${ASSET}"

      - name: Test
        shell: bash
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          BIN="./dist/${{ steps.osinfo.outputs.asset }}"
          chmod +x "$BIN" 2>/dev/null || true

          # Check '--version'
          OUT="$("$BIN" --version)"
          echo "$OUT"
          case "$OUT" in
            *"$VERSION"*) echo "version ok" ;;
            *) echo "::error::version $VERSION not found in output: $OUT" >&2; exit 1 ;;
          esac

          # Check exit 0
          HOME="$(mktemp -d)" "$BIN" < /dev/null
          echo "exit 0"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.osinfo.outputs.asset }}
          path: dist/rune-mcp-*
          if-no-files-found: error
          retention-days: 1

  release:
    name: Publish prerelease
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Resolve version
        id: version
        env:
          DISPATCH_VERSION: ${{ inputs.version }}
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="$DISPATCH_VERSION"
          else
            VERSION="${GITHUB_REF#refs/tags/}"
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist/
          merge-multiple: true

      - name: List and checksum
        run: |
          ls -lh dist/
          (cd dist && sha256sum rune-mcp-* > checksums.txt)
          cat dist/checksums.txt

      - name: Publish prerelease
        if: github.event_name != 'workflow_dispatch' || !inputs.dry_run
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: rune-mcp ${{ steps.version.outputs.version }}
          prerelease: true
          files: |
            dist/rune-mcp-*
            dist/checksums.txt
          fail_on_unmatched_files: true
          generate_release_notes: true
